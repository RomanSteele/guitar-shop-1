export enum AppRoute {
    Main = '/main',
    Item = '',
    NotFound = '/404',
  }

export enum APIRoute {
    Guitars = '/guitars',
  }

export enum ApiType {
    FetchGuitars = 'data/fetchGuitars',
  }
